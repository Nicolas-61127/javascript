// mpm install readline-sync
const readline = require('readline-sync')
    do {
        nota = readline.questionFloat('Digite uma nota : ')
    } while( nota < 0 || nota > 10)
    soma += nota

    media = soma / 2
    console.log(`A média é ${media}`)